/*

An integrated object should map to the given method prop layer
An adapter class receives properties and parameters to direct the
view object.

A created entity is a set of pre-baked parameters, applied on call
through the adapter.
 */

// class DisplayObject {
//     static targetObjectAssignment(cls){
//         /* Return a plural of the type*/
//         return `display`;
//     }
// }

